= form_for @school_group do |f|
  -if @school_group.errors.any?
    #error_explanation
      %h2= "#{pluralize(@school_group.errors.count, "error")} prohibited this school_group from being saved:"
      %ul
        - @school_group.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'

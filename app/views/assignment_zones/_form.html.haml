= form_for @assignment_zone do |f|
  -if @assignment_zone.errors.any?
    #error_explanation
      %h2= "#{pluralize(@assignment_zone.errors.count, "error")} prohibited this assignment_zone from being saved:"
      %ul
        - @assignment_zone.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'

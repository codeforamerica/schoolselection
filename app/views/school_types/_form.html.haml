= form_for @school_type do |f|
  -if @school_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@school_type.errors.count, "error")} prohibited this school_type from being saved:"
      %ul
        - @school_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'

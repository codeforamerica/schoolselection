#favorites
  %h2 My Picks
  - unless session[:favorites].try(:present?)
    To create a customized list of your top choices, click the "Add to My Picks" button.
  - else
    %ol#favorite-list
      - for school in @favorite_schools
        %li= school.name
    = link_to "Compre >", compare_schools_path(:address => params[:address], :zipcode => params[:zipcode], :grade_level => params[:grade_level])
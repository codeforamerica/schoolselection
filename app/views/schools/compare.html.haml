#left-column 
  #page
    %h1.bold
      My Favorites
    
    %table#favorite-schools.listings
      = render :partial => 'schools/favorite_schools', :locals => {:favorite_schools => @favorite_schools}
    
    %br/
    %h3.bold
      = link_to "< Back to List View", schools_path(:address => params[:address], :zipcode => params[:zipcode], :grade_level => params[:grade_level])
        
  = render :partial => 'layouts/page_footer'
  